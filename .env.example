NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-random-string

# Keycloak (realm: cashup-dev)
KEYCLOAK_ISSUER=https://auth.bakso.site/realms/cashup-dev
KEYCLOAK_CLIENT_ID=replace-with-client-id
KEYCLOAK_CLIENT_SECRET=replace-with-client-secret

# Optional: backend base URL if you call a separate API
BACKOFFICE_API=http://localhost:8800
